import {CapacitorConfig} from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "acceptance.test.example",
  appName: "acceptance-test-example",
  webDir: "webapp/build",
  bundledWebRuntime: false
};

export default config;

